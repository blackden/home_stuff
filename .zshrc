export ZSH="$HOME/.oh-my-zsh"
export PATH="$PATH:/usr/local/sbin"
ZSH_THEME="mortalscumbag"
plugins=(git docker vagrant adb ansible brew command-not-found pip helm kubectl minikube gpg-agent oc otp rsync terraform pass)
source $ZSH/oh-my-zsh.sh
